# Pinned dependency versions — update deliberately, not automatically.
# Run `pip install -r requirements.txt` to install exact versions.
# To upgrade: update version here, test, then commit.
fastapi==0.115.12
uvicorn[standard]==0.34.3
anthropic==0.52.0
pydantic==2.11.4
pydantic-settings==2.9.1
sqlalchemy==2.0.41
aiosqlite==0.21.0
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.20
httpx==0.28.1
websockets==14.2
python-dotenv==1.1.0
jinja2==3.1.6
